/**
 * Конфигурация тестов приложения (vitest.config.ts)
 * 
 * Назначение:
 * - Настраивает среду для запуска тестов
 * - Определяет правила выполнения тестов
 * - Подключает дополнительные инструменты тестирования
 * 
 * Основные настройки:
 * - Поддержка браузерного окружения
 * - Доступ к глобальным функциям тестирования
 * - Подключение вспомогательных настроек
 * 
 * Особенности:
 * - Работает вместе с React Testing Library
 * - Эмулирует работу браузера
 * - Автоматически подключает настройки
 */

// Подключаем типы Vitest для конфигурации тестов
/// <reference types="vitest" />

import { defineConfig } from 'vite';          // Функция для создания конфигурации

export default defineConfig({
  test: {
    globals: true,                            // Разрешаем использовать функции тестирования без импорта
    environment: 'jsdom',                     // Имитируем браузер для тестов компонентов
    setupFiles: ['./src/setupTests.ts'],      // Файл с дополнительными настройками тестов
  },
});
