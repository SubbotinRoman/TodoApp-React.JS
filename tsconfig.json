/**
 * Корневой конфигурационный файл TypeScript (tsconfig.json)
 * 
 * Назначение:
 * - Объединяет несколько конфигураций TypeScript
 * - Разделяет настройки для разных частей проекта
 * - Позволяет использовать разные настройки для приложения и сборки
 * 
 * Конфигурации:
 * - tsconfig.app.json: настройки для исходного кода приложения
 * - tsconfig.node.json: настройки для конфигурационных файлов
 * 
 * Особенности:
 * - Использует проектные ссылки TypeScript
 * - Обеспечивает изоляцию настроек
 * - Ускоряет процесс разработки
 */

{
  "files": [],                              // Пустой список файлов (используются только ссылки)
  "references": [                           // Ссылки на другие конфигурации TypeScript
    { "path": "./tsconfig.app.json" },      // Настройки для кода приложения
    { "path": "./tsconfig.node.json" }      // Настройки для конфигурационных файлов
  ]
}
